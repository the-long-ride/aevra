import assert from 'node:assert/strict';import test from 'node:test';import {AevraDatabase} from '../../../packages/store/src/database.js';import {AuditRepository} from '../../../packages/store/src/audit.js';import {AuditService} from '../src/audit/audit-service.js';
test('tampered audit chain identifies first broken event',()=>{const db=AevraDatabase.open(':memory:'),repo=new AuditRepository(db.raw()),a=new AuditService(repo);const x=a.append({operation:'a',result:'ok',redactionCount:0});const y=a.append({operation:'b',result:'ok',redactionCount:0});db.raw().prepare('UPDATE audit_events SET event_json=? WHERE id=?').run('{"operation":"tampered"}',y.id);assert.deepEqual(a.verify(),{valid:false,brokenEventId:y.id});db.close();});
