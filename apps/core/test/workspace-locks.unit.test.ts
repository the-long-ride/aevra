import assert from 'node:assert/strict';import test from 'node:test';import {WorkspaceLockCoordinator} from '../src/policy/workspace-locks.js';
test('read-only concurrency and exclusive source mutation',async()=>{const c=new WorkspaceLockCoordinator();const a=await c.acquire({operationId:'a',sessionId:'s',workspaceId:'w',effect:'READ_ONLY',outputKeys:[]});const b=await c.acquire({operationId:'b',sessionId:'t',workspaceId:'w',effect:'READ_ONLY',outputKeys:[]});let waited=false;const p=c.acquire({operationId:'c',sessionId:'u',workspaceId:'w',effect:'SOURCE_MUTATION',outputKeys:[]},()=>{waited=true});await new Promise(r=>setTimeout(r,15));assert.equal(waited,true);a.release();b.release();(await p).release();});
