import {readdirSync,rmSync,mkdirSync,statSync} from 'node:fs';import path from 'node:path';import type {AevraDatabase} from '../../../../packages/store/src/database.js';
export class BackupService{constructor(private db:AevraDatabase,private dir:string,private keepDaily=7,private keepWeekly=4){}create(kind:'daily'|'weekly'='daily',now=new Date()){mkdirSync(this.dir,{recursive:true});const stamp=now.toISOString().replace(/[:.]/g,'-'),file=path.join(this.dir,`${kind}-${stamp}.db`);this.db.backup(file);this.prune(kind);return file}prune(kind:'daily'|'weekly'){const keep=kind==='daily'?this.keepDaily:this.keepWeekly;const files=readdirSync(this.dir).filter(f=>f.startsWith(`${kind}-`)&&f.endsWith('.db')).map(f=>path.join(this.dir,f)).sort((a,b)=>statSync(b).mtimeMs-statSync(a).mtimeMs);for(const f of files.slice(keep))rmSync(f,{force:true});}}
